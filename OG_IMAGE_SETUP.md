# Open Graph Image Setup

## ✅ Решение проблемы с OG изображением

Проблема была в том, что файл `og-image.png` отсутствовал в директории `public/`.

### Реализованные решения:

#### 1. **Динамическая генерация OG изображений (Рекомендуется)**

Создан API endpoint `/api/og` который генерирует OG изображения динамически:

- **URL**: `https://andrewaltair.ge/api/og`
- **Параметры**:
  - `title` - заголовок (по умолчанию: "Andrew Altair")
  - `description` - описание (по умолчанию: "AI ინოვატორი და კონტენტ კრეატორი")
  - `type` - тип (default/post)

**Примеры использования:**

```typescript
// Главная страница (по умолчанию)
https://andrewaltair.ge/api/og

// Кастомный заголовок
https://andrewaltair.ge/api/og?title=My%20Blog%20Post

// Полная кастомизация
https://andrewaltair.ge/api/og?title=My%20Post&description=Description&type=post
```

**Преимущества:**
- ✅ Автоматическая генерация для каждой страницы
- ✅ Динамический контент
- ✅ Нет необходимости в статических файлах
- ✅ Поддержка грузинского текста
- ✅ Брендированный дизайн с градиентами

#### 2. **Статический генератор (Fallback)**

Создан HTML-генератор для создания статического OG изображения:

**Как использовать:**

1. Открой в браузере: `http://localhost:3000/og-image-generator.html`
2. Отредактируй заголовок и описание
3. Нажми "Download PNG"
4. Сохрани файл как `og-image.png` в `public/`

**Когда использовать:**
- Если нужен статический fallback
- Для тестирования дизайна
- Если edge runtime недоступен

### Текущая конфигурация

Метаданные в `src/app/layout.tsx` обновлены:

```typescript
openGraph: {
  images: [
    {
      url: "https://andrewaltair.ge/api/og",
      width: 1200,
      height: 630,
      alt: "Andrew Altair - AI ინოვატორი და კონტენტ კრეატორი",
    },
  ],
},
twitter: {
  card: "summary_large_image",
  images: ["https://andrewaltair.ge/api/og"],
}
```

### Тестирование

#### Локально:
```bash
npm run dev
# Открой: http://localhost:3000/api/og
```

#### Проверка метатегов:
1. **Facebook Debugger**: https://developers.facebook.com/tools/debug/
2. **Twitter Card Validator**: https://cards-dev.twitter.com/validator
3. **LinkedIn Inspector**: https://www.linkedin.com/post-inspector/

### Дизайн OG изображения

**Размеры**: 1200x630px (стандарт Open Graph)

**Элементы:**
- 🎨 Градиентный фон (purple → cyan)
- 🔲 Сетка паттерн
- 🏷️ Badge с индикатором
- 📝 Заголовок с градиентом
- 📄 Описание
- 📊 Статистика (8+ лет, 50K+ подписчиков, 200+ статей)
- 🌐 Домен внизу справа

**Цвета:**
- Primary: `#6366f1` (Indigo)
- Accent: `#22d3ee` (Cyan)
- Background: `#0a0a0f` (Dark)
- Text: `#e4e4e7` (Light Gray)

### Использование для постов блога

Для динамических OG изображений в постах, добавь в метаданные страницы:

```typescript
// src/app/blog/[slug]/page.tsx
export async function generateMetadata({ params }): Promise<Metadata> {
  const post = await getPost(params.slug);
  
  return {
    openGraph: {
      images: [
        {
          url: `/api/og?title=${encodeURIComponent(post.title)}&description=${encodeURIComponent(post.excerpt)}&type=post`,
          width: 1200,
          height: 630,
        },
      ],
    },
  };
}
```

### Troubleshooting

**Проблема**: Изображение не отображается в соцсетях
- ✅ Проверь, что сайт доступен публично (не localhost)
- ✅ Очисти кеш в Facebook Debugger
- ✅ Убедись, что URL абсолютный (с https://)

**Проблема**: Грузинский текст не отображается
- ✅ Edge runtime поддерживает Unicode
- ✅ Используется системный шрифт с поддержкой Georgian

**Проблема**: Изображение генерируется медленно
- ✅ Edge runtime оптимизирован для скорости
- ✅ Можно добавить кеширование через CDN

### Следующие шаги

1. ✅ Протестируй `/api/og` локально
2. ✅ Задеплой на production
3. ✅ Проверь в Facebook Debugger
4. ✅ Добавь динамические OG изображения для постов
5. ✅ Настрой кеширование (опционально)

---

**Статус**: ✅ Готово к использованию
**Автор**: Andrew Altair AI Assistant
**Дата**: 2026-01-09
