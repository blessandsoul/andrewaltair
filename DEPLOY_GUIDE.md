# 🚀 Инструкция по деплою (для "чайников")

Поздравляю! У тебя есть VPS и код. Теперь давай запустим твой сайт.

## Шаг 1: Подготовка на VPS

1.  **Подключись к VPS** (как ты уже делал):
    ```bash
    ssh root@<IP_ТВОЕГО_СЕРВЕРА>
    ```

2.  **Установи Docker и Docker Compose** (если еще нет):
    Скопируй и вставь эту команду целиком:
    ```bash
    curl -fsSL https://get.docker.com | sh
    ```
    Проверь, что установилось:
    ```bash
    docker --version
    docker compose version
    ```

## Шаг 2: Загрузка файлов

Тебе нужно перекинуть файлы проекта на сервер. Самый простой способ — использовать **FileZilla** или команду `scp`.

**Что нужно загрузить:**
В папку `/root/fresh` (создай её) нужно закинуть:
1.  Папку `src`
2.  Папку `public`
3.  Файл `package.json`
4.  Файл `package-lock.json`
5.  Файл `next.config.mjs`
6.  Файл `tsconfig.json`
7.  Файл `Dockerfile` (который я создал)
8.  Файл `docker-compose.yml` (который я создал)
9.  Файл `.env` (Твой файл с секретами, переименуй `.env.local` или `.env.example` в `.env` и заполни данными!)

**ВАЖНО:** Папку `node_modules` и `.next` **НЕ** грузи! Они создадутся сами внутри Docker.

## Шаг 3: Запуск!

1.  Зайди в папку на сервере:
    ```bash
    cd /root/fresh
    ```

2.  Запусти команду сборки и старта:
    ```bash
    docker compose up -d --build
    ```
    *(Это может занять пару минут, пока скачаются зависимости и соберется проект)*

3.  Проверь, что все работает:
    ```bash
    docker compose logs -f
    ```
    Если видишь `Ready in ...`, значит сайт работает! Нажми `Ctrl+C`, чтобы выйти из логов.

## Шаг 4: Открой сайт

Твой сайт теперь доступен по адресу: `http://<IP_ТВОЕГО_СЕРВЕРА>:3000`

---

### Полезные команды

*   **Остановить сайт:** `docker compose down`
*   **Перезапустить:** `docker compose restart`
*   **Обновить сайт (после загрузки нового кода):**
    ```bash
    docker compose down
    docker compose up -d --build
    ```
