feat(workshop): live per-name multi answers on host pult

During r6 (find-the-mistake, type multi) the host pult showed only the
aggregate bar count, so the host could not read finders' names aloud for
his by-name confirm plus progressive-hint script.

Add a per-name panel for multi rounds, live during the open phase:
- service: computeResults multi branch now returns responders (name plus
  picked labels with a correct flag), built from a single find sorted by
  createdAt so finds read in order; anon handling mirrors collectWriteIns.
- types: new MultiResponder, optional responders on the multi RoundResults.
- pult: MultiResponders component wired into RemoteClient next to PinList;
  one Georgian header string findersHeader.
- fix: valueOf in getRoomHistory resolves multi picks to labels (was blank,
  fell through to textValue), so the History drawer and end-stats stop
  rendering empty for any multi round.

Tests: workshop.service.test.ts covers responders mapping with anon plus
correct flags and the getRoomHistory multi label join.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
